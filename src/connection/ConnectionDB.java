package connection;import java.sql.DriverManager;import java.sql.ResultSet;import java.sql.Statement;public class ConnectionDB {    public static void main(String[] args) {        String databaseURL = "jdbc:postgresql://localhost:5432/stockmanagerment";        try {            Class.forName("org.postgresql.Driver");            java.sql.Connection connection= DriverManager.getConnection(databaseURL,"postgres","Nin220304");            Statement sm=connection.createStatement();            //sm.execute("CREATE TABLE stockmanagementdb(id serial,name varchar(100),price float,qty )");            // sm.execute("ALTER TABLE person ADD COLUMN salary int" );            //sm.executeUpdate("INSERT INTO person(name,salary) VALUES ('Nin',1000)");            ResultSet rs=sm.executeQuery("SELECT * FROM stockmanagementdb");            while (rs.next()){                int id=rs.getInt("id");                String name=rs.getString("name");                float price=rs.getFloat("price");                int qty=rs.getInt("qty");                //String time= rs.getString("time");                System.out.println(id+" " + name +" " +price+" "+qty);            }        } catch (Exception e) {            System.out.println("Error");            System.out.println(e.getMessage());        }    }}